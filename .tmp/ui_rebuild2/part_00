<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#fff8ef">
  <title>NER แจ่วฮ้อน - Smart Recipe</title>
  <script src="https://cdn.jsdelivr.net/npm/xlsx/dist/xlsx.full.min.js"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap');

    :root {
      --cream: #fff8ef;
      --cream-2: #f8ecdf;
      --paper: rgba(255,255,255,.96);
      --paper-solid: #fffdf9;
      --brown: #6b4635;
      --brown-dark: #3f2c25;
      --brown-soft: #9c7b67;
      --line: #ead8c8;
      --line-strong: #dcc5b0;
      --green: #6e805b;
      --green-soft: #edf3e9;
      --rose: #ca5663;
      --rose-soft: #fff0f2;
      --text: #1f2937;
      --muted: #7d8490;
      --shadow: 0 12px 32px rgba(76, 52, 39, .08);
      --shadow-strong: 0 24px 64px rgba(52, 36, 28, .20);
      --radius-xl: 28px;
      --radius-lg: 20px;
      --radius-md: 15px;
    }

    * { box-sizing: border-box; }
    html, body { margin: 0; min-height: 100%; background: var(--cream); }
    body {
      font-family: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at 7% 5%, rgba(239,211,188,.35), transparent 24rem),
        radial-gradient(circle at 95% 16%, rgba(210,223,189,.32), transparent 28rem),
        linear-gradient(180deg, #fff9f1 0%, #fffdf8 48%, #f9efe4 100%);
      overflow-x: hidden;
    }
    button, input { font: inherit; }
    button { -webkit-tap-highlight-color: transparent; }

    /* Decoration is fully isolated from interactions. */
    #decor-layer {
      position: fixed;
      inset: 0;
      overflow: hidden;
      pointer-events: none !important;
      user-select: none;
      z-index: 0;
    }
    .decor-person {
      position: absolute;
      display: block;
      pointer-events: none !important;
      user-select: none;
      background-image: url('assets/ner-team-bg.webp');
      background-repeat: no-repeat;
      background-size: 540px auto;
      opacity: .82;
      filter: saturate(.92) contrast(.98);
    }
    .decor-a { width: 145px; height: 195px; left: -105px; top: 178px; background-position: left top; -webkit-mask-image: linear-gradient(90deg,#000 55%,transparent 100%); mask-image: linear-gradient(90deg,#000 55%,transparent 100%); }
    .decor-b { width: 145px; height: 195px; left: -105px; top: 1000px; background-position: left bottom; opacity: .64; -webkit-mask-image: linear-gradient(90deg,#000 55%,transparent 100%); mask-image: linear-gradient(90deg,#000 55%,transparent 100%); }
    .decor-c { width: 145px; height: 220px; right: -105px; top: 680px; background-position: right center; opacity: .70; -webkit-mask-image: linear-gradient(270deg,#000 55%,transparent 100%); mask-image: linear-gradient(270deg,#000 55%,transparent 100%); }

    #app-shell {
      position: relative;
      z-index: 10;
      width: min(1180px, calc(100% - 28px));
      margin: 0 auto;
      padding: max(22px, env(safe-area-inset-top)) 0 calc(96px + env(safe-area-inset-bottom));
    }

    .hero {
      border: 1px solid var(--line);
      background: var(--paper);
      box-shadow: var(--shadow);
      border-radius: var(--radius-xl);
      padding: 26px;
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
    }
    .hero-row {
      display: flex;
      gap: 20px;
      align-items: center;
      justify-content: space-between;
    }
    .brand-title {
      margin: 0;
      font-size: clamp(26px, 4.2vw, 40px);
      line-height: 1.15;
      font-weight: 800;
      letter-spacing: -.03em;
      color: #131b2d;
    }
    .brand-title .accent { color: #5b4fde; }
    .brand-subtitle {
      margin: 10px 0 0;
      color: #7c8492;
      line-height: 1.6;
      font-size: 14px;
      font-weight: 500;
    }
    .manager-btn {
      min-height: 52px;
      padding: 0 22px;
      border: 0;
      border-radius: 17px;
      background: linear-gradient(135deg, #6d4a39, #432e25);
      color: #fff;
      font-weight: 800;
      font-size: 14px;
      box-shadow: 0 10px 25px rgba(67,46,37,.18);
      cursor: pointer;
      white-space: nowrap;
    }
    .manager-btn:active { transform: scale(.985); }

    #app-menus { margin-top: 34px; }
    .category-block { margin-bottom: 34px; }
    .category-title {
      display: flex;
      align-items: center;
      gap: 10px;
      margin: 0 0 14px;
      padding: 13px 16px;
      color: var(--brown-dark);
      font-size: 21px;
      font-weight: 800;
      border: 1px solid rgba(234,216,200,.72);
      border-radius: 18px;
      background: rgba(255,249,242,.86);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
    }
    .category-icon {
      width: 34px;
      height: 34px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      background: #f1dfcf;
      font-size: 17px;
      flex: 0 0 auto;
    }
    .menu-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 14px;
    }
    .menu-card {
      min-height: 116px;
      width: 100%;
      border: 1px solid #e7d8cb;
      border-radius: 20px;
      background: rgba(255,255,255,.97);
      box-shadow: 0 8px 22px rgba(76,52,39,.055);
      padding: 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      text-align: left;
      color: inherit;
      cursor: pointer;
      transition: transform .15s ease, box-shadow .15s ease, border-color .15s ease;
    }
    .menu-card:hover { border-color: #cdb39e; box-shadow: 0 12px 28px rgba(76,52,39,.10); transform: translateY(-1px); }
    .menu-card:active { transform: scale(.992); }
    .menu-card-title { font-size: 17px; font-weight: 800; color: #3e312b; line-height: 1.3; }
    .menu-card-subtitle { margin-top: 5px; color: #9a8b7e; font-size: 12px; font-weight: 700; }
    .menu-card-subtitle.single { color: var(--green); }
    .menu-arrow {
      width: 38px; height: 38px; flex: 0 0 38px;
      border-radius: 50%; display: grid; place-items: center;
      color: var(--brown); background: #f8eee5; font-size: 25px; line-height: 1;
    }
    .loading, .empty-state {
      padding: 64px 20px;
      text-align: center;
      color: #9aa0aa;
      font-weight: 700;
    }

    /* Modal foundation: no dependency on Tailwind's hidden class. */
    .modal {
      position: fixed;
      inset: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 14px;
      background: rgba(52,43,38,.60);
      backdrop-filter: blur(7px);
      -webkit-backdrop-filter: blur(7px);
    }
    .is-hidden { display: none !important; }
    .modal-card {
      width: min(1120px, 100%);
      max-height: 94vh;
